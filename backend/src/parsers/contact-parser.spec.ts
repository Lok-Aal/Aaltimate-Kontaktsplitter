import ContactParser from './contact-parser-interface';



describe('ContactParser parses correctly for good Input', () => {

    let parser: ContactParser;

    beforeEach(() => {
        
    });

    it('Parses: "Herr Dr. med. Max Mustermann"', () => {
                
    });
});